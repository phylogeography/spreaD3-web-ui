(ns spread-ui.handlers-tests
  (:require  [cljs.test :refer-macros [deftest is testing run-tests]]
             [finch-ui.handlers :as handlers]))

(deftest handlers-tests
  (testing "test."
    (let []
      (is (= 1 1)))))
